<%@ page contentType="text/html;charset=UTF-8" %><%@ taglib prefix="c" uri="jakarta.tags.core" %><html><head><title>Danh sách</title><style>body{font-family:Arial;margin:30px}table{border-collapse:collapse;width:100%}th,td{border:1px solid #ccc;padding:8px}</style></head><body><h2>Danh sách sinh viên</h2><p>Người dùng: <b>${sessionScope.username}</b> (${sessionScope.role})</p><form method="get" action="${pageContext.request.contextPath}/students"><input name="keyword" value="${keyword}" placeholder="Tìm theo họ tên"><button>Tìm kiếm - Bài 6</button></form><br><c:if test="${sessionScope.role == 'ADMIN'}"><a href="${pageContext.request.contextPath}/student-form.jsp">+ Thêm sinh viên</a></c:if><br><br><c:if test="${empty students}"><p style="color:red">Không tìm thấy sinh viên phù hợp.</p></c:if><table><tr><th>Mã</th><th>Họ tên</th><th>Lớp</th><th>Email</th><th>Thao tác</th></tr><c:forEach var="sv" items="${students}"><tr><td>${sv.id}</td><td>${sv.name}</td><td>${sv.className}</td><td>${sv.email}</td><td><c:if test="${sessionScope.role == 'ADMIN'}"><a href="${pageContext.request.contextPath}/student-edit?id=${sv.id}">Sửa</a><form action="${pageContext.request.contextPath}/student-delete" method="post" style="display:inline"><input type="hidden" name="id" value="${sv.id}"><button onclick="return confirm('Xóa?')">Xóa</button></form></c:if><c:if test="${sessionScope.role != 'ADMIN'}">Chỉ xem</c:if></td></tr></c:forEach></table><p><a href="${pageContext.request.contextPath}/dashboard">Dashboard</a> | <a href="${pageContext.request.contextPath}/logout">Đăng xuất</a></p></body></html>
